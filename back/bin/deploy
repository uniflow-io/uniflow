#!/bin/sh

open -a "Google Chrome" https://app.buddy.works/darkwood-fr/uniflow-io/pipelines

: 'ssh admin@ssh.darkwood.fr << CMD

# deploy back
cd /var/www/uniflow.io

#sudo service mysql restart
#sudo service varnish restart
#sudo service nginx restart
#sudo service redis restart
#redis-cli flushall

git pull
(cd back;composer install --optimize-autoloader)
(cd back;php bin/console cache:clear  --no-debug)
(cd back;php bin/console doctrine:schema:update --force)

CMD'

# bin/deploy_front
# bin/deploy_bash
