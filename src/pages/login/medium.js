import React from 'react'
import {LoginMedium} from '../../views'
import Layout from "../../layouts";

export default ({location}) => <Layout location={location}><LoginMedium location={location}/></Layout>
